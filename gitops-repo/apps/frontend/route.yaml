apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend
  namespace: app-prod
spec:
  parentRefs:
    - name: platform-gateway
      namespace: default
  rules:
    - backendRefs:
        - name: frontend
          port: 80
